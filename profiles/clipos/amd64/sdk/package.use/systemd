# We are in a SDK: systemd package is emerged only for the tooling it provides,
# not for managing services. Therefore, disable as much as possible and enable
# only firstboot and sysusers.
sys-apps/systemd -cryptsetup -curl -gcrypt -gnuefi -http -idn -ssl -sysv-utils firstboot sysusers
